global:
  - name: Install Bun 1.3.3
    run: npm install -g bun@1.3.3
  - name: Install Claude Code CLI
    run: npm install -g @anthropic-ai/claude-code
repo:
  - name: Install dependencies
    run: bun install --frozen-lockfile
