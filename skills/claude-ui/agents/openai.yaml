interface:
  display_name: "Claude UI"
  short_description: "Delegate UI edits to Claude Code."
  default_prompt: "Use $claude-ui to implement a focused UI change directly in this checkout."

policy:
  allow_implicit_invocation: true
